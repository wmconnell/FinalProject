import { SquadUserPipe } from './squad-user.pipe';

describe('SquadUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SquadUserPipe();
    expect(pipe).toBeTruthy();
  });
});
