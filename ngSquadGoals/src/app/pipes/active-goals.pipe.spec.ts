import { ActiveGoalsPipe } from './active-goals.pipe';

describe('ActiveGoalsPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveGoalsPipe();
    expect(pipe).toBeTruthy();
  });
});
